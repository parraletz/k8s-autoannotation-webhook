apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autoannotation.fullname" . }}-test-connection"
  labels:
    {{- include "autoannotation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autoannotation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
